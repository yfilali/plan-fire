# Copy to `.env` and fill in to enable the live AI co-pilot.
# Get a key at https://console.anthropic.com → Settings → API Keys.
# Compose auto-loads `.env`; `.env` is gitignored so the key is never committed.
ANTHROPIC_API_KEY=sk-ant-...
